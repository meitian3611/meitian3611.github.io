# 2026-08-26 博客内容抢救（meitian3611.github.io）

## 背景
用户的 GitHub Pages 博客（D:\DevStudy\meitian3611.github.io）是 Hexo 4.0 + NexT 7.4.2 (Gemini) 的**纯构建产物仓库**，源 .md 文件已丢失（hexo deploy 直推，历史提交全是 "Site updated"）。用户确认源文件丢失，选择"先只抢救内容，框架以后再定"。

## 做了什么
- 从 `search.xml`（382KB，含全部正文 HTML CDATA）+ `content.json`（元数据）逆向恢复 38 篇文章为 Markdown
- 关键坑：search.xml 的 URL 是百分号编码中文，需 `urllib.parse.unquote` 后才能与 content.json 的明文 path 匹配（否则 28/38 篇拿不到元数据）
- NexT 特有结构预处理：`figure.highlight > table` 代码块 → fenced code（按 `<br>` 切行，围栏长度自适应代码内反引号）；删除 heading 内空锚点 `a.headerlink`；`<a id="more">` → `<!-- more -->`
- 残留 HTML 检测必须**排除 fenced code block 内部**，否则代码示例里的 `<div v-format=...>` 会误报（首版 13 篇误报，实际 0 残留）
- 11 张外链图片（i.loli.net ×10、cdn.nlark.com ×1）全部 HEAD 200 且已下载备份

## 产物位置（工作区外！）
`D:\DevStudy\blog-recovery\`
- `posts/` 38 篇 .md（文件名 `YYYY-MM-DD-标题.md`，frontmatter 含 title/date/tags/source 原始 URL）
- `images/`（站点资源副本）、`images-external/`（外链图片备份）
- `manifest.json`（机器可读索引：字数/代码块数/图片/校验状态）
- `recovery-report.md`（人类可读报告）
- `recover.py`（可复现脚本，venv：`C:\Users\v_tianmei\.workbuddy\binaries\python\envs\default`，依赖 bs4/markdownify/lxml）

## 后续
- 用户后续可能迁移 Astro / VitePress，manifest.json 可直接当导入索引用
- 迁移时注意：旧 URL 格式 `/:year/:month/:day/:title/`，frontmatter 的 source 字段保留了原始链接
- 未做任何 git 操作；建议用户自行将 blog-recovery 纳入版本管理

---

# 2026-08-26（下午）Astro 博客搭建完成

## 决策
用户经方案对比（Next/Nuxt/Astro + 流行度调研）后选定 **Astro 自建博客**（非模板主题）。

## 新项目位置
`D:\DevStudy\blog-astro\`（Astro 5，site=meitian3611.github.io）
- 38 篇文章已迁入 `src/posts/`，11 张外链图本地化到 `public/images/posts/` 并改写 md 内链接（`migrate.py`）
- 路由完全对齐旧站：文章 `/:year/:month/:day/:title/`（slug 从 frontmatter source 提取）、分页 `/page/N/`、标签 `/tags/<名>/`、归档 `/archives/`
- 验证 38/38 URL 对齐（`verify-urls.py`），构建 85 页，preview 全 200

## 关键坑（重要）
1. **`new URL().pathname` 会把中文路径百分号编码**，Astro 直接拿编码串当目录名落盘 → 生成了字面量 `%E8%A3%85` 目录导致 28 篇 404。修复：`decodeURIComponent(pathname)` 后再返回 slug
2. **沙箱禁止写 `AppData\Local\npm-cache`**，npm install 挂起 20 分钟无产出。修复：`--cache="D:/DevStudy/blog-astro/.npm-cache"` + npmmirror 镜像，24 秒装完
3. `.astro` 文件移动目录后 import 相对路径要同步改（posts/[...slug].astro → pages/[...slug].astro）

## 部署架构（等用户执行）
- 源码推 `source` 分支，GitHub Actions（`.github/workflows/deploy.yml`）构建后 peaceiris/actions-gh-pages 推 `main`（force_orphan）
- main 保持产物分支（Pages 指向不变），线上站切换零风险
- 用户需自行执行：git init → remote add → push source 分支（硬性规则：不代做 git 操作）
